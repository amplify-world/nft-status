# Base URL for the NFT status API backend
# Copy this file to .env.local and set your values
VITE_API_BASE_URL=http://localhost:8000

# IPFS gateway base URL (no trailing slash) — defaults to https://ipfs.io
# For Pinata dedicated gateway: https://[your-subdomain].mypinata.cloud
VITE_IPFS_GATEWAY=
